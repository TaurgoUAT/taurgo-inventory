import 'dart:math';

import 'dart:io';


// String generateUniqueReference() {
//   final uuid = Uuid(); // Create an instance of the UUID class
//   String uniqueId = uuid.v4().split('-').last; // Take the last part of the UUID
//   DateTime now = DateTime.now();
//   String formattedDate = '${now.minute}${now.second.toString().padLeft(2, '0')}';
//   return 'TAURGO-$uniqueId';
// }

// String generateUniqueReferenceForPartners() {
//   int lastId =  ?? 1001 - 1;
//   int nextId = lastId + 1;
//   return 'TAURGO-$nextId';;
// }

// String referenceNumber = generateUniqueReference();
//
// List<String> status = ['pending', 'completed', 'rejected'];
